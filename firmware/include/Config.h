#pragma once

// Safe initial defaults only. Verify these pins against the exact dev-board
// pinout and silkscreen before applying power.
constexpr int SWITCH_PIN = 4;
constexpr int LED_PIN = 5;
constexpr bool SWITCH_ACTIVE_LOW = true;
