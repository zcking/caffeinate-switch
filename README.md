# Caffeinate Switch

A USB desk rocker switch for controlling a Mac-owned `caffeinate` process.

The shared serial wire contract is documented in [protocol/PROTOCOL.md](protocol/PROTOCOL.md).
The initial Swift protocol library lives in `mac/CaffeinateSwitch`.
